package com.arcane.game.Actions;

//This interface is named Performance instead of Action, in order to avoid conflict with existing classes.
public interface Performance{
    public boolean perform();
}